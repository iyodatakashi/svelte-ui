/// <reference types="@vitest/browser/matchers" />
/// <reference types="@vitest/browser/providers/playwright" />

// Register vitest-browser-svelte to inject render/cleanup on the page context
import 'vitest-browser-svelte';
